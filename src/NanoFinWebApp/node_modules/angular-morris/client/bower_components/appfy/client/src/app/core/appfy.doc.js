/**
 * @ngdoc overview
 * @name index
 * @requires ngStorage
 * @requires ngAnimate
 * @requires ngSanitize
 * @requires ngLocale
 * @requires ngLodash
 * @requires appfy.setting
 * @requires appfy.enviroment
 * @requires appfy.i18n
 * @requires appfy.utils
 * @requires appfy.user
 * @requires appfy.page
 * @requires ui.router
 * @requires satellizer
 * @description
 * 
 * > Appfy client is the frontend application. It works exclusively with `Angular.js` and requires some killer libs like `ui.router` to manage routes and `satellizer` for user session management.
 * 
 * 
 * #Requirements
 * - Node 4+
 * - Npm 2+
 * - Bower 1.7+
 * - Gulp 3.8+
 * 
 * #Installation
 * For production purpose
 * ```
 * $ npm install -g appfy
 * ``` 
 * For development of Appfy or CLI, just clone the corresponding repository.
 * ```
 * $ git clone https://github.com/Appfy/appfy
 * //or
 * $ git clone https://github.com/Appfy/appfy-cli
 * ``` 
 * ### Contributing with CLI
 * After cloning just run `npm link` inside appfy-cli folder. This way you'll can work with Appfy CLI anywhere.
 * 
 * ### Contributing with Appfy
 * After clonning issue `gulp tasks` on client folder to verify all the tasks.
 **/